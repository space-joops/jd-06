# 아스트로펫 게임 개발 안내서

> 📘 **개발을 이어받는 에이전트는 [`docs/DEVELOPMENT.md`](docs/DEVELOPMENT.md)를 반드시 읽을 것** — 아키텍처 상세, 설계 규약, 튜닝 수치, 검증 방법, 배포 절차, 추가 개발 계획이 정리되어 있다. 이 파일(AGENT.md)은 세계관과 확정 방향의 요약본이다.

# 배경
- 인류는 우주 쓰레기 문제로 우주여행의 문제에 봉착한다. 동시에 우주쓰레기를 먹는 우주생명체와 조우하게 된다. 이 우주 생명체는 애완동물 처럼 사람의 보살핌이 있을 때 더 많은 우주 쓰레기를 먹어 치우며 우주를 정화한다. 인류의 과학자들은 우주생명세 아스토로펫의 알을 채집하여 개인에게 분양한다. 어느 정도 지구 환경에서 유대감을 쌓고 우주 쓰레기 처리 임무를 더 효과적으로 수행하기 위한 장비와 슈트를 입혀서 우주로 발사한다. 인류는 우주애적인 관점에서 펫을 분양 받아 키우며 인류의 우주 발전에 기여한다는 자부심을 갖게 된다. 아스트로펫은 사람의 애정과 보살핌을 지속적으로 받아야 건강하게 임무를 수행 할수 있다. 특히 지구괴도를 돌면서 우주쓰레기를 처리하기에 주인이 있는곳 상공 반경 어느 정도에 주기적으로 오며 이때 돌봄과 협업을 통해 우주쓰레기 처리를 극대화 할수 있다. 이런 수집 활동을 통해 얼마나 많은 우주 쓰레기를 처리하고 있는지를 자랑하기도 한다.

# 확정 방향성 (2026-07 브레인스토밍)
1. **감성 돌봄 중심** — 숫자 성장보다 펫과의 교감이 핵심. 죽음/처벌 없음, 방치하면 시무룩해질 뿐. "그리움과 재회의 리듬"을 파는 힐링 게임.
2. **압축 시간** — 궤도 한 바퀴 3분. 재회 윈도우는 우리 집 상공(정점) 기준 좌우 45°(궤도의 25% ≈ 45초).
3. **깔끔한 플랫/SVG 스타일** — 에셋 파일 없이 코드로 그리는 SVG 캐릭터. 다크 우주 그라디언트 + 파스텔 포인트.
4. **실제 운영 지향** — 모바일 퍼스트, 단계적으로 서버/푸시/소셜 확장.

# 핵심 루프
알 분양 → 알 품어 부화 → 이름 짓기 → 지구에서 육성(유대감 100 달성) → 슈트 입혀 발사 → 궤도 수거 루프
- 궤도 루프 = 자동 수거(기분에 비례) + 궤도마다 재회 윈도우(쓰다듬기·간식·협동 수거 미니게임) + 우주 편지 수신 + 도감 수집

# 시간 설계
- 궤도 주기 3분. 재회 윈도우 = 우리 집 상공(정점, phase 0)을 가운데로 좌우 45°(= 궤도의 25%, ≈45초)
- 상공 통과 기준 "재회 패스"마다 간식·협동 수거 각 1회 (phase 0 경계를 걸쳐도 한 번으로 묶임)
- 발사 직후 phase 0 = 상공 한가운데 → 즉시 재회 상태 (첫 재회 UX)
- Phase 1은 클라이언트 시간(Date.now) 기준. Phase 2에서 서버 시간 검증으로 이전 (치팅 방지)
- 야간 수면 궤도(알림 무음 시간대)는 Phase 3 푸시 도입 시 설계

# 감정 시스템
- 기분(mood) 0~100. 8시간에 걸쳐 100→0 자연 감소, 상호작용으로 회복
- 낮은 기분은 시무룩한 표정과 수거 효율 저하로만 표현 (효율 계수 0.25 + 0.75 × mood/100)
- 표정 매핑: 재회 윈도우·발사 = excited / 70+ = happy / 40~70 = neutral / 40 미만 = lonely

# 교신(편지) 시스템 — 킬러 피처
- 궤도에서 45분마다 펫이 편지 1통을 보냄. 기분 50 이상이면 밝은 톤, 미만이면 그리운 톤 템플릿
- 발사 직후 웰컴 편지 1통. 오프라인 복귀 정산 시 최대 3통
- 편지가 쌓이면 그 자체가 펫과의 앨범이 됨

# 수거와 도감
- 기분 100 기준 궤도당 6개 자동 수거.
- "함께 수거하기" = 우주유영 아케이드 게임(SpacewalkGame): 화면을 끌어 가상 조이스틱으로 분사 이동하며 사방의 우주쓰레기를 수거. 분사 가스가 0이 되면 종료(초기 가스는 `NEXT_PUBLIC_GAS_MAX`, 기본 70). 유명 위성 10종(본체에 영어 약칭, 스타링크는 SpaceX)이 플라이바이하며 근접하면 가스 충전 + 화면 흔들림·"웅" 사운드. 붉은 위험물은 충돌 시 가스 감소. 기분 충전 아이템도 등장. 모은 파편은 도감/총량에 반영.
- 실행 경로 2가지: ① 재회 윈도우 버튼(재회당 1회, 기분 +10 보너스·패스 소비) ② 설정의 "지금 수거하러 가기"(언제나·무제한, 패스 미소비)
- 도감 7종: 페인트 조각·나사와 볼트(흔함) / 단열재 파편·로켓 페어링(보통) / 태양전지판 조각·폐위성(희귀) / 우주인의 공구가방(전설 — 2008년 우주유영 중 실제 분실 사건 모티브)
- 10분 이상 부재 후 복귀하면 정산 모달("그동안 N개 수거, 편지 M통")

# 비주얼
- 다크 우주 그라디언트(#0b1026 → #241a4d) + 파스텔(민트 #7de8c3 / 핑크 #f9a8d4 / 라벤더 #c4b5fd / 별노랑 #ffe9a8)
- 캐릭터: 젤리 블롭 + 큰 눈 + 안테나. 색상 3종(=알 선택), 표정 6종(neutral·happy·excited·lonely·sleepy·eating), 슈트 레이어(헬멧/배낭/배지, 색 3종)
- 상호작용 생동감: 재회 윈도우 밖에선 펫이 쓰레기를 먹는 먹방 애니(`PetEating`), 쓰다듬으면 스퀴시 반응(WAAPI) + 진동(Vibration API)
- 모바일 퍼스트: max-w 430px 중앙 프레임, 100dvh

# 기술 로드맵
- **Phase 1 (완료)**: Next.js 15 App Router + localStorage 싱글플레이. 코어 루프 완성
- **Phase 2 (완료)**: PWA — manifest/아이콘/서비스워커, 홈 화면 설치 안내(beforeinstallprompt + iOS 가이드), 새 버전 업데이트 배너, 재회 윈도우 로컬 알림, 앱 버전 표시(package.json과 빌드 시 동기, `NEXT_PUBLIC_APP_VERSION`)
  - 로컬 알림 한계: 탭이 열려 있는(백그라운드 포함) 동안만 동작. 앱을 완전히 닫았을 때의 푸시는 Phase 3 서버 도입 후 지원
  - SW 캐시는 `sw.js?v=<버전>` 쿼리로 버전링 — 배포 시 package.json 버전을 올리면 업데이트 플로우가 동작
- **Phase 3 (차후)**: 계정 + DB(Supabase 등) + 서버 시간 검증 + Web Push("펫이 5분 뒤 상공을 지나요")
- **Phase 4**: 소셜 — 리더보드, 인류 누적 정화량 게이지 (공유 카드·OG 프리뷰는 v0.6.0에서 선반영)

# 소셜 공유 (v0.6.0)
- 설정의 "친구에게 자랑하기": 카카오톡·페이스북·X·인스타그램·링크복사 + "카드로 자랑하기"
- 웹 표준 우선: Open Graph/Twitter 메타(`layout.tsx`, 정적 `public/og.png` 1200×630), Web Share
  API/Level 2(이미지 공유), Clipboard, 표준 share-intent URL. 카카오톡만 공식 SDK(키 있을 때).
- "카드로 자랑하기" = 캔버스로 펫(`PetSvg` 재사용)+정화량 이미지를 만들어 Web Share files로 공유
  (미지원 시 다운로드). 인스타그램은 웹 인텐트가 없어 이 이미지 공유가 유일한 심리스 경로.
- 공유 로직은 `src/lib/share.ts`, UI는 `src/components/panels/SharePanel.tsx`.
- env: `NEXT_PUBLIC_SITE_URL`(OG 절대경로), `NEXT_PUBLIC_KAKAO_JS_KEY`(카카오).

# 다국어 (i18n, v0.8.0)
- **10개 언어 지원**: 한국어(원문)·English(폴백)·العربية(**RTL**)·中文·日本語·Español·Français·Deutsch·Português·Русский. UI 전체 + 긴 편지·도감 설명까지 **전체 번역**.
- **자동 감지 + 설정 전환**: `navigator.languages` 기본 서브태그로 자동 선택(미지원 언어는 en 폴백). 설정 최상단 "언어 / Language 🌐" 섹션에서 네이티브 표기 목록으로 즉시 변경, `localStorage`(`astropet-locale`)에 저장·재로드 유지.
- **경량 커스텀 i18n**(무거운 dep 없이 PWA·localStorage 모델 유지): `src/i18n/config.ts`(지원 언어·감지), `messages/{code}.ts`(카탈로그, `ko.ts`가 구조 원본 `type Messages = typeof ko`), `I18nProvider.tsx`(감지·`t(key,params)`·`formatNumber`/`formatDate`·`document.documentElement.lang/dir` 갱신). `Game.tsx`가 `<I18nProvider>`로 감싼다.
- **아랍어 RTL**: provider가 `dir` 설정, 논리 클래스(`ms-`/`me-`/`text-start`)와 `rtl:` 변형으로 방향 보정. **캔버스 게임 월드·궤도 시각화는 미러링하지 않음**(공간 게임; HUD 텍스트만 번역).
- **편지 i18n**: `Letter.tkey`(템플릿 키)를 저장 → 렌더 시 현재 언어로 번역. 레거시 세이브는 `title/body` 폴백(마이그레이션 불필요).
- **폰트**: `globals.css`·공유 카드 캔버스에 CJK/아랍 시스템 폰트 폴백(웹폰트 미번들).
- **한계**: 서버 메타데이터(`layout.tsx`)·PWA `manifest.ts`·`og.png`는 요청별 로케일 전환이 어려워 **기본 로케일(en) 1개**로 고정. 번역은 초안 수준으로 원어민 감수는 후속 과제.

# 코드 구조 (Phase 1)
- `src/lib/` — 순수 게임 로직 (types, constants 튜닝 수치, game.ts 정산/궤도/액션, letters.ts 편지 템플릿, storage.ts)
- `src/hooks/useGame.ts` — 1초 틱, 오프라인 정산, 자동 저장
- `src/components/` — PetSvg(캐릭터), OrbitView(궤도 시각화), SpacewalkGame(우주유영 수거 게임, 캔버스+rAF), screens/(스테이지별 화면), panels/(편지함·도감·정산 모달)
- `src/lib/satellites.ts` — 우주유영 게임에 등장하는 유명 위성 10종 정의 + 캔버스 드로잉
- 게임 수치를 바꿀 땐 `src/lib/constants.ts`만 수정하면 됨
- 우주젤리 쿨다운은 환경 변수 `NEXT_PUBLIC_FEED_COOLDOWN_MS`(ms)로 재정의 가능 — `.env.example` 참고 (빌드 시 반영)
- 모든 화면 하단 중앙에 앱 버전이 상시 표기됨 (Game.tsx 공통 푸터)
